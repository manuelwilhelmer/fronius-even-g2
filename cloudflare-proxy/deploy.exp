#!/usr/bin/expect -f
set timeout -1
spawn npx wrangler deploy
expect "Would you like to register a workers.dev subdomain now?"
send "y\r"
expect "What would you like your workers.dev subdomain to be?"
send "manuel-proxy-[expr {int(rand()*10000)}]\r"
expect "Ok to proceed?"
send "y\r"
expect eof
